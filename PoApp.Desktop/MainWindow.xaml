<Window x:Class="PoApp.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ASME PO Assistant"
        Height="900"
        Width="1400"
        MinHeight="720"
        MinWidth="1100">
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="520" />
            <ColumnDefinition Width="12" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Purchase Order Context" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,10" />

                <CheckBox Content="Code Use (ASME construction)"
                          IsChecked="{Binding CodeUse, Mode=TwoWay}"
                          Margin="0,0,0,10" />

                <TextBlock Text="Governing Standard" FontWeight="SemiBold" />
                <ComboBox ItemsSource="{Binding GoverningStandards}"
                          SelectedItem="{Binding SelectedGoverningStandard, Mode=TwoWay}"
                          Margin="0,6,0,0" />

                <TextBox Text="{Binding OtherGoverningStandard, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,6,0,10">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ShowOtherGoverningStandard}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <CheckBox Content="MTR Required"
                          IsChecked="{Binding MtrRequired, Mode=TwoWay}"
                          Margin="0,0,0,6" />
                <TextBlock Text="{Binding PolicyStatus}"
                           TextWrapping="Wrap"
                           Foreground="#444"
                           FontSize="12"
                           Margin="0,0,0,14" />

                <TextBlock Text="Material Spec" FontWeight="SemiBold" />
                <ComboBox ItemsSource="{Binding Specs}"
                          SelectedItem="{Binding SelectedSpec, Mode=TwoWay}"
                          DisplayMemberPath="AsmeSpec"
                          IsEditable="True"
                          IsTextSearchEnabled="True"
                          Margin="0,6,0,10" />

                <TextBlock Text="Title" FontWeight="SemiBold" />
                <TextBox Text="{Binding TitleDisplay}" IsReadOnly="True" Margin="0,6,0,10" />

                <TextBlock Text="ASTM Identical" FontWeight="SemiBold" />
                <TextBox Text="{Binding AstmDisplay}" IsReadOnly="True" Margin="0,6,0,10" />

                <TextBlock Text="Ordering Requirements" FontWeight="SemiBold" Margin="0,6,0,6" />
                <TextBlock Text="{Binding ValidationStatus}"
                           TextWrapping="Wrap"
                           Foreground="#666"
                           FontSize="12"
                           Margin="0,0,0,8" />

                <Border BorderBrush="#D0D0D0" BorderThickness="1" CornerRadius="6" Padding="8">
                    <ItemsControl ItemsSource="{Binding OrderingFields}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#E2E2E2" BorderThickness="0,0,0,1" Padding="0,8">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Label}" TextWrapping="Wrap" FontWeight="SemiBold" />
                                        <TextBlock Text="{Binding RequiredHint}" FontSize="11" Foreground="#666" Margin="0,2,0,4" />

                                        <TextBox Text="{Binding TextValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                            <TextBox.Style>
                                                <Style TargetType="TextBox">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding UsesTextInput}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBox.Style>
                                        </TextBox>

                                        <ComboBox ItemsSource="{Binding Options}"
                                                  SelectedItem="{Binding SelectedOption, Mode=TwoWay}"
                                                  Margin="0,2,0,0">
                                            <ComboBox.Style>
                                                <Style TargetType="ComboBox">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding HasSingleSelectOptions}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </ComboBox.Style>
                                        </ComboBox>

                                        <CheckBox Content="Selected"
                                                  IsChecked="{Binding BooleanValue, Mode=TwoWay}"
                                                  Margin="0,2,0,0">
                                            <CheckBox.Style>
                                                <Style TargetType="CheckBox">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsBoolean}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                            <Setter Property="IsThreeState" Value="True" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </CheckBox.Style>
                                        </CheckBox>

                                        <Grid Margin="0,2,0,0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="8" />
                                                <ColumnDefinition Width="140" />
                                            </Grid.ColumnDefinitions>

                                            <TextBox Grid.Column="0"
                                                     Text="{Binding NumberValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                            <ComboBox Grid.Column="2"
                                                      ItemsSource="{Binding Units}"
                                                      SelectedItem="{Binding SelectedUnit, Mode=TwoWay}" />
                                            <Grid.Style>
                                                <Style TargetType="Grid">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsNumberWithUnit}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Grid.Style>
                                        </Grid>

                                        <ItemsControl ItemsSource="{Binding MultiSelectOptions}" Margin="0,2,0,0">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <CheckBox Content="{Binding Value}"
                                                              IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                              Margin="0,2,0,0" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                            <ItemsControl.Style>
                                                <Style TargetType="ItemsControl">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsMultiSelect}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </ItemsControl.Style>
                                        </ItemsControl>

                                        <TextBlock Text="{Binding Note}"
                                                   TextWrapping="Wrap"
                                                   FontSize="11"
                                                   Foreground="#666"
                                                   Margin="0,4,0,0">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding HasNote}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Border>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Content="Regenerate" Command="{Binding RegenerateCommand}" Padding="12,8" Margin="0,0,8,0" />
                    <Button Content="Export" Command="{Binding ExportCommand}" Padding="12,8" Margin="0,0,8,0" />
                    <Button Content="Copy" Command="{Binding CopyToClipboardCommand}" Padding="12,8" />
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <Border Grid.Column="2" BorderBrush="#D0D0D0" BorderThickness="1" CornerRadius="6" Padding="12">
            <StackPanel>
                <TextBlock Text="Purchase Order Output" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,10" />
                <TextBox Text="{Binding GeneratedText}"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         TextWrapping="Wrap"
                         FontFamily="Consolas"
                         FontSize="13"
                         Height="760" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
